TrieClass

nextChild
nextSibling
get
add



Node:

set Val
set child
set sibling


public void add( String newKey ) {

  char[] key = newKey.toCharArray();
  DLBTrieNode root = this.getRoot();
  int end = newKey.length();

  for( int i = 0; i < end; i++ ) {

    if( key[i] == root.getVal() )
    {
      root = root.getChild();
    } else {

      while( root.getSibling().getVal() < key[i] )
      {
        root = root.getSibling();
      }

      if( )
    }


  }


}
